每次RST2清空后，都会默认跳转到00微指令018110，在下一时刻就面临分支，即等待由控制台SWA,SWB选择，接下来是，读，写还是执行程序
仿真图示例中，SWA=1,SWB=0,意味着进入写（执行微程序，即接下来默认的一串微指令21,24,30再回到21），即将一组指令（程序）写入RAM中，示例仅执行一次微程序，再次回到21时，对uaddr清空，并跳转到读操作(20,22,27)，将RAM中的指令读出,
后续执行过程和上述类似,相同的00后20,但是跳转到23,